import React from "react";
import { Redirect, Route } from "react-router";

const PrivateRoute = ({  }) => { 
  
  
  
  return (
    // <Route
    //   {...rest}
    //   render={({ location }) =>
    //     (conditions) ? (
    //       <Component {...rest} />
    //     ) : (
    //       <Redirect
    //         to={{
    //           pathname: "/sign-in",
    //           state: {
    //             from: location,
    //           },
    //         }}
    //       />
    //     )
    //   }
    // />
  );
};

export default PrivateRoute;
