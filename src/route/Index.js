import React from "react";
import { BrowserRouter, Routes } from "react-router-dom";

const Index = () => {
    return (
        <div>
            <BrowserRouter>
                <Routes></Routes>
            </BrowserRouter>
        </div>
    );
};

export default Index;
